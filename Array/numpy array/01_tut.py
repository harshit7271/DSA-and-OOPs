from numpy import *

val = array([1, 2, 3, 4, 5, 6.5, 7, 8, 'a', 'b'])

for i in val:
    print(i, end=" ")

# numpy allows us to create hetrogeneous array where we don't have to mention the type of numbver in it
